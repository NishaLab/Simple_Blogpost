<% (1..4).each do |id| %>
    $('#<%= @react.micropost_id %>-react-count-<%= @react.image_id %>').html("<%= Reaction.where(micropost_id: @react.micropost_id, image_id: @react.image_id).count %>")
<% end %>
